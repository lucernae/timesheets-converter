language: python

virtualenv:
  system_site_packages: true

python:
  - "2.7"

install:
  - pip install -e .
  - pip install -r travis-requirements.txt

script:
  - flake8 timesheets
  - tox

after_success:
  - cd timesheets
  - codecov
